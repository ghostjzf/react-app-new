import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

export default () => {
  return <Header style={{ padding: 0, backgroundColor: '#fff', height: '48px' }}>111</Header>;
};
